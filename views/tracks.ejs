<h1>Tracks</h1>
<% tracks.forEach(track => { %>
    <h2><%- track.name %></h2>
    <% if (track.preview_url) { %>
    <audio controls>
        <source src="<%= track.preview_url %>" type="audio/mpeg">
    </audio>
    <% } else { %>
        <p>No preview</p>
        <% } %>
<% }) %>

